def tokenbucket():
    n = int(input('enter number of packets to be sent :'))
    ch = 1
    x = 0
    m = 0
    a = []
    while (n>=x):
        ch = int(input('enter 1 to send the packet :'))
        if (ch == 1):
            print("f'{x+1}th packet is sent")
        else:
            print('packet is not sent')
    print(f{x}, 'packets are not sent')
    print('f{m}packets are sent')
tokenbucket()
